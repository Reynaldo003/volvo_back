from django.apps import AppConfig


class ChecklistEntregaConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "checklist_entrega"
    verbose_name = "Checklist de entrega de vehículo"
